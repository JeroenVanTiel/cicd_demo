trigger:
- none

pool:
  vmImage: ubuntu-latest

variables:
- group: "Custom Terraform Vars DEV"

stages:
  - stage: test
    jobs:
    - job:
      displayName: Retrieve variables
      steps:
        - task: Powershell@2
          displayName: Variable retrieval
          inputs:
            targetType: inline
            script: |
              $SqlAdminVarName = '$(tenant-id)' + "-admin-username"
              Write-Host "Direct access:  $($env:$sdlz-metadata-d-sqldb-001-admin-password)"